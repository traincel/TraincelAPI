import express from 'express';

const router = express.Router();

router.get('/getCountries', (req, res, next) => {

});

router.get('/addCountries', (req, res, next) => {

});

export default router;